#include <stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    char star = '*';
    for (int i = 1; i <= n; i++)
    {
        printf("\n");
        for (int j = if (condition)
        {
            /* code */
        }
        else if (condition)
        {
            /* code */
        }
        else
        {
            /* code */
        }
        ; j >=; j--)
        {
            printf("%c", star);
        }
    }
}